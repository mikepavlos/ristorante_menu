version: '3.9'

volumes:
  db_data:

services:
  db:
    image: postgres:15.1-alpine
    volumes:
      - db_data:/var/lib/postgresql/data/
    env_file:
      - .env
    hostname: localhost
    ports:
      - '6000:5432'
